name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cd ./vsvincent && npm install pnpm -g && npm install && firebase experiments:enable webframeworks && pnpm build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          entryPoint: './vsvincent/'
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_VINCENTVONSCHILLING }}'
          channelId: live
          projectId: vincentvonschilling
